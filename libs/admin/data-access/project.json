{
    "name": "admin-data-access",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/admin/data-access/src",
    "prefix": "poms",
    "projectType": "library",
    "tags": ["scope:admin", "type:data-access"],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{workspaceRoot}/dist/out-tsc/libs/admin/data-access"],
            "options": {
                "command": "pnpm exec tsc -b libs/admin/data-access/tsconfig.lib.json --pretty false"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        }
    }
}
