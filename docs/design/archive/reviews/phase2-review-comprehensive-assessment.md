# POMS 第二阶段正式审阅综合评估

**文档状态**: Archived
**最后更新**: 2026-04-04
**适用范围**: `POMS` 第二阶段 `LX-T03` 四轮正式审阅问题的历史综合评估结果与优先级判断留痕
**关联文档**:

- `phase2-review-checklist.md`
- `phase2-review-record-round1.md`
- `phase2-review-record-round2.md`
- `phase2-review-record-round3.md`
- `phase2-review-record-round4.md`
- `../../contract-finance-design.md`
- `../../phase2-commission-freeze-at-handover.md`
- `../../phase2-project-unified-accounting-view-caliber.md`
- `../../phase2-lx-t04-full-mainline-development-decision.md`

---

## 1. 文档目标

本文档用于保留四轮正式审阅发现的 22 个问题从“已记录”推进到“已形成可执行综合判断”时的正式评估结果。

2026-04-04 起，本文档转入 `archive/reviews/`。其综合评估结论已被 `../../phase2-mainline-delivery-plan.md`、`../../phase2-mainline-implementation-design-matrix.md`、`../../phase2-lx-t04-full-mainline-development-decision.md` 与 `../../poms-design-progress.md` 吸收，当前不再作为默认开发入口。

本文档重点回答以下问题：

- 22 个问题分别值不值得优先处理
- 哪些问题会直接阻断第二阶段进入最终统一开发判断
- 哪些问题虽重要，但可进入受控 follow-up
- 哪些问题已因最新设计决策而降级或转入规则细化

本文档不是替代 `phase2-review-checklist.md` 的执行入口，而是作为正式审阅问题的综合评估结果。

---

## 2. 评估维度与口径

本轮综合评估分为两层：

- 第一层是“主评估维度”，用于回答问题值不值得优先处理
- 第二层是“实施型补充维度”，用于回答问题之间能否并行、是否可隔离、应先收口什么

### 2.1 主评估维度

主评估维度统一采用以下 6 个维度：

1. 业务价值
   修复后对制度正确性、业务闭环、协作效率和经营可信性的提升程度。

2. 技术复杂度
   对对象模型、版本 / 快照、查询视图、权限口径和上下游读写链路的改动复杂度。

3. 紧迫性
   对最终统一开发判断的阻断程度，以及是否必须在进入开发前先收口。

4. 影响面
   波及的主线、角色、对象与跨文档范围。

5. 返工风险
   若带着当前缺口进入实现，后续回改模型、规则或读侧的概率与成本。

6. 当前优先级
   统一采用以下标记：

- `P0`：进入最终统一开发判断前必须收口，属于实现前硬阻断
- `P1`：高优先级 follow-up，原则上应进入首批细化 / 实现范围
- `P2`：可受控后置，不阻断首批实现，但需明确范围限制或后续 owner
- `P3`：当前阶段非核心范围或纯表达增强，不应抢占主线资源

### 2.2 实施型补充维度

除上述 6 个主评估维度外，当前还应补充以下 5 个实施型维度：

1. 问题依赖关系
   判断某个问题是否依赖其他问题先收口，或是否本身是后续多个问题的前置。

2. 决策成熟度
   判断该问题当前处于“业务政策未定”“方向已收口待细化”“规则待回写”还是“可直接进入实现”。

3. 范围可隔离性
   判断该问题能否通过明确范围限制、试点条件或已知限制安全后置。

4. 验证与验收成本
   判断该问题是否需要跨时间点、跨角色、跨版本或跨合同进行复杂验证，而不仅是写完规则即可。

5. 数据回填 / 历史兼容冲击
   判断该问题是否会影响历史数据解释、既有台账迁移、快照重算或旧记录回补。

这些补充维度不直接替代主评估表，但必须进入实施排序判断。

### 2.3 观察型补充维度

除上述内容外，以下两个维度建议作为实现排期讨论时的观察项：

- 发布原子性：是否必须跨 `L3/L4/L5` 同步落地，还是可拆分上线
- 组织协同成本：是否需要财务、商务行政、销售、管理层共同拍板后才能冻结规则

补充说明：

- 本轮在原有 7 个正式审阅维度之外，额外加入“交易链条完整性 / 可追溯性”和“返工不可逆性”作为综合评估参考。
- 2026-04-01 已明确：多币种暂列未来扩展；一个 `Project` 必须支持多份同时履约的有效销售合同；项目级交易链条最小覆盖范围包括销售合同、应收计划、回款、销项发票、成本、付款、进项发票。

---

## 3. 当前总判断

基于最新已确认的设计决策，22 个问题当前可分为：

- `P0`：11 项
- `P1`：7 项
- `P2`：3 项
- `P3`：1 项

当前最重要的变化有三点：

1. `R4-005` 已不再是“是否支持多合同”的开放问题，而是“多合同项目如何在 `L3/L4/L5` 落成稳定规则”的收口问题。
2. `R1-001` 已明确降为未来扩展，不再占用当前阶段核心优先级。
3. 交易链条完整性已被正式识别为进入实现前必须核对的新判断维度，因此涉及合同、应收、回款、成本、付款和发票链路的问题整体优先级上升。

---

## 4. 逐项多维度评估表

| 编号     | 业务价值 | 技术复杂度 | 紧迫性 | 影响面                              | 返工风险 | 当前优先级 | 最新判断                                                                         |
| -------- | -------- | ---------- | ------ | ----------------------------------- | -------- | ---------- | -------------------------------------------------------------------------------- |
| `R1-001` | `中`     | `中`       | `低`   | `L1`，涉外 / 外币项目               | `低`     | `P3`       | 已明确为未来扩展；当前只需避免把单币种假设固化成不可逆模型                       |
| `R1-002` | `极高`   | `高`       | `高`   | `L2 ~ L5`，财务 / 经营 / 提成       | `高`     | `P0`       | 已完成第二批首轮写回；当前重点转主线稳定消费与一致性收口                         |
| `R1-003` | `高`     | `中高`     | `中高` | `L3 ~ L5`                           | `高`     | `P1`       | 已完成第三批首轮写回与 `L2/L3` 联动补点；当前重点转跨文档一致性复核              |
| `R1-004` | `极高`   | `高`       | `高`   | `L4 ~ L5`，财务口径                 | `高`     | `P0`       | 已完成第二批首轮写回；当前重点转主线稳定消费与一致性收口                         |
| `R1-005` | `中`     | `低`       | `低`   | `L5`，销售解释体验                  | `低`     | `P2`       | 属于提成解释表达增强，不阻断主事实链                                             |
| `R1-006` | `极高`   | `中高`     | `高`   | 横切权限、敏感经营数据              | `高`     | `P0`       | 已完成第一批七层映射回写；当前作为全主线实现设计的前置输入                       |
| `R2-001` | `极高`   | `中`       | `高`   | `L1 ~ L5`                           | `高`     | `P0`       | 已完成第一批七层映射回写；当前作为合同主链与下游主线的前置输入                   |
| `R2-002` | `极高`   | `中`       | `高`   | `L2 ~ L4`                           | `高`     | `P0`       | 已完成第二批首轮写回；当前重点转主线稳定消费与一致性收口                         |
| `R2-003` | `高`     | `高`       | `中高` | `L4`，月末回看 / 对账 / 审批回看    | `中高`   | `P1`       | 已完成第二批首轮写回；当前重点转主线稳定消费与一致性收口                         |
| `R2-004` | `极高`   | `中高`     | `高`   | `L5`，第二阶段发放判断              | `高`     | `P0`       | 已完成第一批七层映射回写；当前作为 `L5` 与后续争议链的前置输入                   |
| `R2-005` | `高`     | `中`       | `中高` | 横切权限 / 审计闭环                 | `中`     | `P1`       | 已完成第三批首轮写回；当前重点转跨文档一致性复核                                 |
| `R3-001` | `高`     | `中`       | `中高` | `L1`                                | `中高`   | `P1`       | 已完成第三批首轮写回；当前重点转跨文档一致性复核                                 |
| `R3-002` | `极高`   | `高`       | `高`   | `L2 ~ L5`                           | `高`     | `P0`       | 已完成第一批七层映射回写；当前作为成本可信源与第二批收口的前置输入               |
| `R3-003` | `中高`   | `高`       | `低中` | `L3`，大型项目 / 多期合同           | `中`     | `P2`       | 可先明确“当前仅支持整体移交”的范围限制，后续再扩展分期移交                       |
| `R3-004` | `高`     | `低中`     | `中`   | `L4 ~ L5`，管理判断                 | `中`     | `P1`       | 已完成第二批首轮写回；当前重点转主线稳定消费与一致性收口                         |
| `R3-005` | `高`     | `中`       | `中`   | 横切协作 / 通知触发                 | `中`     | `P2`       | 已有待办和站内通知基线，当前更适合作为规则复用与触发策略专题                     |
| `R3-006` | `高`     | `高`       | `中`   | `L5`，争议处理与公平性感知          | `中高`   | `P1`       | 已完成第三批首轮写回并补齐 `L5 gate` 联动；当前重点转跨文档一致性复核            |
| `R4-001` | `极高`   | `中`       | `高`   | `L1 ~ L5`                           | `高`     | `P0`       | 已完成第一批七层映射回写；当前作为合同放行与下游主线的前置输入                   |
| `R4-002` | `极高`   | `高`       | `高`   | `L2 ~ L5`                           | `高`     | `P0`       | 已完成第二批首轮写回，并已补齐移交前再基线化与执行期基线衔接；当前待稳定消费收口 |
| `R4-003` | `极高`   | `中`       | `高`   | `L4 ~ L5`，财务 / 管理层 / 审批留痕 | `高`     | `P0`       | 已完成第二批首轮写回与 `L5 gate` 联动补点；当前重点转主线稳定消费与一致性收口    |
| `R4-004` | `高`     | `中`       | `中高` | 横切审批、导出 / 打印               | `中高`   | `P1`       | 已完成第三批首轮写回；当前重点转跨文档一致性复核                                 |
| `R4-005` | `极高`   | `高`       | `高`   | `L3 ~ L5`，合同 / 经营 / 提成       | `高`     | `P0`       | 已完成第一批七层映射回写；当前作为多合同主假设与项目级冻结策略的前置输入         |

---

## 5. 问题依赖与成熟度矩阵

为避免只按 `P0 / P1 / P2 / P3` 线性排序，当前还应补一层依赖、成熟度与可隔离性判断。

| 编号     | 主要依赖 / 被依赖                                             | 决策成熟度           | 范围可隔离性       | 验证 / 历史兼容提示                                          | 实施判断                                               |
| -------- | ------------------------------------------------------------- | -------------------- | ------------------ | ------------------------------------------------------------ | ------------------------------------------------------ |
| `R1-001` | 基本独立；不被当前 `P0` 主链依赖                              | 方向已收口为未来扩展 | 可通过范围限制隔离 | 只需验证单币种模型保留扩展缝                                 | 当前不进入首批实施                                     |
| `R1-002` | 依赖 `R3-002`；被 `R1-004`、`R2-003`、`R3-004`、`R4-003` 依赖 | 已完成首轮收口       | 不可隔离           | 仍需继续做历史共享成本、分摊依据与聚合口径一致性验证         | 已完成第二批首轮写回，当前转一致性收口                 |
| `R1-003` | 依赖 `R4-001`、`R4-002`；被 `L3/L5` 收口链消费                | 已完成首轮收口       | 局部可隔离         | 仍需继续做签后变更、移交前再基线化一致性验证                 | 已完成第三批首轮写回，当前转一致性复核                 |
| `R1-004` | 依赖 `R1-002`、`R3-002`；被 `R2-003`、`R3-004`、`R4-003` 依赖 | 已完成首轮收口       | 不可隔离           | 仍需继续做财务对账、税务差异与历史口径一致性验证             | 已完成第二批首轮写回，当前转一致性收口                 |
| `R1-005` | 依赖 `R3-006`、`R4-004`                                       | 规则待细化           | 可通过范围限制隔离 | 主要是展示与解释验证                                         | 可后置                                                 |
| `R1-006` | 上游前置；被 `R2-005`、`R4-004`、`R3-005` 依赖                | 已完成七层收口       | 不可隔离           | 仍需继续做角色矩阵、脱敏与导出边界一致性验证                 | 已完成第一批七层映射回写，当前作为全主线前置输入       |
| `R2-001` | 上游前置；被 `R4-001`、`R1-003`、`R2-004` 依赖                | 已完成七层收口       | 不可隔离           | 仍需继续做从 `签约就绪` 生成快照与应收计划的一致性验证       | 已完成第一批七层映射回写，当前作为合同主链前置输入     |
| `R2-002` | 依赖成本来源与治理规则；被 `R2-003`、`R3-004`、`R4-003` 依赖  | 已完成首轮收口       | 不可隔离           | 仍需继续做历史成本阶段归属与稳定派生一致性验证               | 已完成第二批首轮写回，当前转一致性收口                 |
| `R2-003` | 依赖 `R1-004`、`R2-002`；被 `R3-004`、`R4-003` 依赖           | 已完成首轮收口       | 局部可隔离         | 仍需继续做月末、关账、补录与历史回看一致性验证               | 已完成第二批首轮写回，当前转一致性收口                 |
| `R2-004` | 上游前置；被 `R4-003`、`R3-006` 依赖                          | 已完成七层收口       | 不可隔离           | 仍需继续做验收对象、阶段成果确认与证据链一致性验证           | 已完成第一批七层映射回写，当前作为 `L5` 前置输入       |
| `R2-005` | 依赖 `R1-006`；被 `R4-004`、`R3-005` 依赖                     | 已完成首轮收口       | 局部可隔离         | 仍需继续做申请、揭示、到期失效与审计留痕一致性验证           | 已完成第三批首轮写回，当前转一致性复核                 |
| `R3-001` | 与 `R1-003` 并行；被 `L1` 真流程闭环依赖                      | 已完成首轮收口       | 局部可隔离         | 仍需继续做否决 / 重估负路径一致性验证                        | 已完成第三批首轮写回，当前转一致性复核                 |
| `R3-002` | 上游前置；被 `R1-002`、`R1-004`、`R2-002` 依赖                | 已完成七层收口       | 不可隔离           | 仍需继续做成本率版本、生效时间、锁定 / 追溯一致性验证        | 已完成第一批七层映射回写，当前作为成本可信源前置输入   |
| `R3-003` | 基本独立；只影响大型项目 / 多期合同                           | 范围限制可定         | 可通过范围限制隔离 | 主要是范围说明与已知限制验证                                 | 当前可后置                                             |
| `R3-004` | 依赖 `R2-003`；被 `R4-003` 消费                               | 已完成首轮收口       | 局部可隔离         | 仍需继续做低回款比例场景、阈值与展示策略一致性验证           | 已完成第二批首轮写回，当前转一致性收口                 |
| `R3-005` | 依赖 `R1-006`、`R2-005`、`R4-004`；被多主线协作体验消费       | 方向已收口待细化     | 可局部隔离         | 需跨角色通知脱敏、聚合与触发策略验证                         | 横切增强后置                                           |
| `R3-006` | 依赖 `R2-004`、`R4-003`；与 `L5` 计算链耦合                   | 已完成首轮收口       | 局部可隔离         | 仍需继续做争议、回溯、替代版本与已发放影响一致性验证         | 已完成第三批首轮写回并补齐 `L5 gate` 联动              |
| `R4-001` | 依赖 `R2-001`；被 `R1-003`、`R4-002` 影响                     | 已完成七层收口       | 不可隔离           | 仍需继续做差异字段、阈值与重审场景一致性验证                 | 已完成第一批七层映射回写，当前作为合同放行前置输入     |
| `R4-002` | 依赖 `R4-005`、`R1-003`；被 `R4-003` 和 `L4/L5` 依赖          | 已完成首轮收口       | 不可隔离           | 仍需继续做原始基线 / 变更包基线并存与重算一致性验证          | 已完成第二批首轮写回，当前转一致性收口                 |
| `R4-003` | 依赖 `R2-004`、`R2-003`、`R3-004`、`R4-002`                   | 已完成首轮收口       | 不可隔离           | 仍需继续做审批矩阵和同项目跨页面一致性验证                   | 已完成第二批首轮写回，当前转一致性收口                 |
| `R4-004` | 依赖 `R1-006`、`R2-005`；被审批流线上化和 `R3-005` 依赖       | 已完成首轮收口       | 局部可隔离         | 仍需继续做审批场景字段包、导出 / 打印一致性验证              | 已完成第三批首轮写回，当前转一致性复核                 |
| `R4-005` | 上游前置；被 `R2-001`、`R4-002`、`R4-003` 和 `L3/L4/L5` 依赖  | 已完成七层收口       | 不可隔离           | 仍需继续做项目汇总与合同拆解双视角、冻结模式与钻取一致性验证 | 已完成第一批七层映射回写，当前作为多合同主假设前置输入 |

---

## 6. 关键依赖链与实施排序判断

仅靠主评估表还不足以决定“先做什么”，当前至少应识别以下依赖链。

### 6.1 合同与提成主链

建议按以下顺序理解：

`R4-005 -> R2-001 -> R4-001 -> R1-003 -> R4-002 -> R4-003`

判断理由：

- 不先收口多合同项目主假设，就无法稳定定义项目级合同汇总、回款比例与提成 gate
- 不先收口 `签约就绪 -> ContractTermSnapshot / ReceivablePlan`，合同放行与应收计划都缺正式承接对象
- 不先收口放行基线与合同草案差异校验，签后变更与变更基线都会建立在漂移事实之上
- 最终 `R4-003` 的 `L4 -> L5` 绑定矩阵只能建立在上述合同和变更口径稳定后

### 6.2 成本可信源主链

建议按以下顺序理解：

`R3-002 -> R1-002 -> R2-002 -> R1-004 -> R2-003 -> R3-004 -> R4-003`

判断理由：

- 内部成本率治理不定，人力成本就没有可信基础
- 分摊成本规则不定，共享成本无法进入项目级可信口径
- 阶段归属不定，历史回看、毛利率解释和提成 gate 都会漂移
- 税务影响、时点快照口径和公式边界行为必须建立在稳定的成本可信源之上

### 6.3 权限与审批可见性主链

建议按以下顺序理解：

`R1-006 -> R2-005 -> R4-004 -> R3-005`

判断理由：

- 不先明确敏感经营数据边界，例外查看与审批摘要都无法安全设计
- 不先明确例外查看和短时揭示机制，审批页就只能在“信息不够”和“越权暴露”之间摇摆
- 不先稳定审批字段包，通知与协作触发也无法判断哪些内容可推送、哪些必须脱敏

### 6.4 可并行与可后置判断

第三批典型项当前已从“可并行细化”推进到“首轮写回已完成”，现阶段应继续做一致性复核：

- `R1-003`：已完成再基线化规则与 `L2/L3` 联动补点，继续做一致性复核
- `R3-001`：已完成 `L1` 受控回退与负路径写回，继续做一致性复核
- `R2-005` 与 `R4-004`：已完成例外查看 / 短时揭示与审批摘要字段包写回，继续做一致性复核
- `R3-006`：已完成冻结后争议路径与 `L5 gate` 联动写回，继续做一致性复核

当前可通过范围限制后置的典型项：

- `R1-001`：明确当前不覆盖外币项目
- `R3-003`：明确当前仅支持整体移交
- `R1-005`：暂不把异常摘要做到第一批必需体验

### 6.5 建议实施排序

当前更稳妥的实施排序建议如下：

1. 第一批：主对象与主事实前置
   `R4-005`、`R2-001`、`R4-001`、`R2-004`、`R3-002`、`R1-006`

2. 第二批：经营与成本可信源前置
   `R1-002`、`R2-002`、`R1-004`、`R4-002`、`R2-003`、`R3-004`、`R4-003`

3. 第三批：流程健壮性与审批增强
   `R1-003`、`R3-001`、`R2-005`、`R4-004`、`R3-006`

4. 第四批：受控后置或范围限制
   `R1-001`、`R3-003`、`R1-005`、`R3-005`

当前判断：

- 第一批和第二批不应倒置
- 若第一批未收口，不应继续推进全主线实现设计
- 第三批当前已完成首轮写回并转入跨文档一致性复核
- 第四批必须在路线图中以范围限制或 follow-up 的方式显式记录，不能默默忽略

---

## 7. 当前进入最终统一开发判断前必须优先收口的专题

基于上表，当前最应优先推进的不是再扩写新的主线，而是先把以下专题写成正式规则：

1. `签约就绪 -> ContractTermSnapshot / ReceivablePlan` 结构化承接
2. 多合同项目下的项目汇总、合同拆解与提成 gate 解释规则
3. 执行中合同变更后的经营基线与补充估算基线
4. 成本可信源治理：执行阶段归属、分摊规则、内部成本率、税务影响
5. 第二阶段验收 / 阶段成果确认对象与证据链
6. `L4 -> L5` 经营信号到提成 gate 的绑定矩阵
7. 敏感数据边界、审批摘要字段包与例外查看路径

---

## 8. 当前可受控后置的内容

以下内容当前不建议抢占主线资源，但必须保留明确口径：

1. 多币种与汇率扩展
2. 分期移交
3. 提成异常调整摘要展示
4. 跨角色主动通知的细粒度触发策略

这些内容并非不重要，而是当前优先级低于交易链条完整性、经营可信性和提成 gate 稳定性。

---

## 9. 当前结论

第二阶段四轮正式审阅的问题，当前已经不再是“有没有问题”，而是“哪些问题必须先收口，哪些可受控后置”。

最新综合判断是：

- 第一批、第二批与当前范围内第三批专题已完成实现设计收口并进入统一主线闭环
- `LX-01` 最终一致性复核已确认当前范围内未发现阻断 `LX-T04` 的跨文档冲突
- `LX-T04` 的当前正式判断口径统一见 `../../phase2-lx-t04-full-mainline-development-decision.md`，且本轮结论已切换为 Go

因此，本文件当前继续作为第二阶段正式审阅后的优先级判断底稿；其角色不再是证明“尚不能进入开发”，而是证明当前统一开发判断所依赖的阻断项已完成收口或显式后置。
